---
# GlusterFS Stage 1: Core Setup
# Imports shared base setup and prepares storage for GlusterFS

# --- IMPORT SHARED BASE SETUP ---
- import_playbook: ../../shared/playbooks/cluster_base_setup.yml

# --- PREPARE STORAGE FOR GLUSTERFS BRICKS ---
- import_playbook: ../../shared/playbooks/prepare_compute_storage.yml
  vars:
    target_folder: "/data/glusterfs"
    mount_prefix: "volume"
